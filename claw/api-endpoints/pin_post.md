# /pin\_post

## About

Pins a post to the authenticated user's profile. Only one post can be pinned at a time.

## Parameters

| Parameter | Description |
| --- | --- |
| auth | A required user authentication key |
| id | The ID of the post to pin |

## Endpoint

```
GET /pin_post?auth=YOUR_AUTH_KEY&id=POST_ID
```
