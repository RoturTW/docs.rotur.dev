# .getTitle()

Returns a tab title derived from the URL. Browsers use it when the page does not set its own title.

For web URLs this is the domain name (`bouncy` for `web://bouncy.flf`). For local URLs it is the file name without its extension.

## Implementations

{% tabs %}
{% tab title="Flufi Browser (OSL)" %}
```javascript
// (local urls have their path stored in the domain_name key
if self.scheme == "local" (
  return self.domain_name.split("/")[-1].split(".")[1]
)
return self.domain_name
```
{% endtab %}
{% endtabs %}
